import {
  Stack,
  useTheme
} from "@mui/material";

const SubScreen = () => {
  const theme = useTheme();

  return (
    <Stack
      sx={{
        // backgroundColor: "#80db80",
        // height: "100vh",
        // display: "flex",
        // direction: "ltr",
        flexDirection: "row",
        flex: 1,
        // justifyContent: "center",
        // alignItems: "center",
      }}
    >
    
  
    </Stack>
  );
};

export default SubScreen;
